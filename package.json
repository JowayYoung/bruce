{
	"name": "@yangzw/bruce",
	"type": "module",
	"scripts": {
		"build": "pnpm recursive exec -- npm run build",
		"clean": "npm run clean:module && npm run clean:output",
		"clean:module": "rimraf packages/us/node_modules && rimraf node_modules pnpm-lock.yaml && pnpm recursive exec -- rimraf node_modules pnpm-lock.yaml",
		"clean:output": "pnpm recursive exec -- rimraf coverage dist",
		"commit": "git add --all . && git-cz",
		"init": "npm i -g commitizen cz-customizable rimraf && pnpm i -r",
		"mini": "ts-node-esm tasks/mini.ts && pnpm recursive exec -- npm run mount",
		"rebuild": "npm run clean && npm run init && npm run build && npm run mini"
	},
	"config": {
		"cz-customizable": {
			"config": "czrc.cjs"
		}
	},
	"engines": {
		"node": ">=18.0.0",
		"npm": ">=8.6.0"
	},
	"dependencies": {},
	"devDependencies": {
		"@types/node": "20.10.4",
		"@yangzw/bruce-us": "workspace:*",
		"terser": "5.26.0",
		"ts-node": "10.9.2",
		"typescript": "5.3.3"
	},
	"workspaces": [
		"packages/*"
	],
	"private": true
}