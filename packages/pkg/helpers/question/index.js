import { NAME, SCOPE } from "./input-question.js";
import { CHECK_AUTH, CHECK_DEPS, CHECK_ENV, CHECK_PUBLISH } from "./task-question.js";

export {
	CHECK_AUTH,
	CHECK_DEPS,
	CHECK_ENV,
	CHECK_PUBLISH,
	NAME,
	SCOPE
};